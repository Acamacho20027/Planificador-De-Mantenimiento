// Legacy duplicate stub removed. This file intentionally left minimal to avoid
// conflicts with the primary `inspeccion.js` at the project root.
// The real wizard logic lives in `/inspeccion.js` (project root).
// Keeping this stub prevents 404s for any paths that still reference `js/inspeccion.js`.

/* no-op */
